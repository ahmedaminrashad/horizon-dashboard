// Central enum of user roles used throughout the application
// instead of hard-coding role IDs in components.

export const ROLES = Object.freeze({
  ADMIN: 'admin',
  CLINIC: 'clinic',
  DOCTOR: 'doctor',
  PATIENT: 'patient',
  STAFF: 'staff',
  OTHER: 'other',
  SUPER_ADMIN: 'super_admin',
})

